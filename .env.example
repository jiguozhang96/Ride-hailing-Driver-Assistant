# 复制此文件为 .env（或直接设置系统环境变量），填入你的密钥。
# 这些密钥只存在服务端，不会暴露到前端。

# 和风天气（REST，走后端代理隐藏）
QW_HOST=your_qweather_api_host
QW_KEY=your_qweather_api_key

# 高德「Web服务」key（REST 天气/POI/地理编码，走后端代理隐藏）
AMAP_WS_KEY=your_amap_webservice_key

# 服务端口（默认 3000）
# PORT=3000
